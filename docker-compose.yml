version: "3.7"

services:
  zyvue:
    networks:
      - frontend
    image: senthilsweb/zyvue:latest
    container_name: zyvue
    restart: unless-stopped
    tty: true
    ports:
      - "3333:80"
    environment:
      - REST_ENDPOINT=http://localhost:2222/api/
networks:
  frontend: